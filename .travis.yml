dist: bionic

jobs:
  include:
  - language: csharp
    dotnet: "3.1"
    mono: none
    rvm: 2.7

script:
 - dotnet restore
 - dotnet build
 - dotnet pack -c Release
 - ruby ./scripts/test_files_are_importable_by_msbuild.rb
 - 'unzip -l bin/Release/*.nupkg'
